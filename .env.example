# TronOS Docker Environment Variables
# Copy this file to .env and update with your values

# AI API Key (required for AI features)
TRONOS_API_KEY=your-api-key-here

# AI Provider: anthropic or openai
TRONOS_AI_PROVIDER=anthropic

# AI Model to use
# For Anthropic: claude-sonnet-4-6, claude-opus-4-6, etc.
# For OpenAI: gpt-4, gpt-4-turbo, etc.
TRONOS_AI_MODEL=claude-sonnet-4-6

# When set, the browser client signs every API request so the server can verify authenticity.
# Note: VITE_ prefix vars are embedded in the frontend bundle and visible to users.

# Optional: For HTTPS with Traefik (uncomment traefik service in docker-compose.yml)
# ACME_EMAIL=your-email@example.com
